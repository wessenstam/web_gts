# Variables for the probe container
server_ip='192.168.1.168'
server_prt=5000
user_name=admin
passwd="CE@Nutanix12"

# Set via --env-file ./env.list check_ip with the ip address of the to be checked PC